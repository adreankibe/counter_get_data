const general_reports = (data,report)=>{
    if(report !== null)
    {

        // update exisiting report table
    }
    else if(report === null)
    {
        // save new report table
    }


}
module.exports = general_reports